# Python cache
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments
env/
venv/
ENV/
env_isaaclab/
.venv

# Training outputs (IMPORTANT: Don't commit training data!)
runs/
*.pt
*.pth
*.onnx
*.pkl
*.ckpt

# Experiment logs
*.log
events.out.tfevents.*
tensorboard/

# Isaac Sim cache and temp files
*.usda
*.usdc
_isaac_sim/
.nvidia-omniverse-logs/
kit_logs/

# IDE settings
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store

# System files
Thumbs.db
desktop.ini

# Jupyter notebooks checkpoints
.ipynb_checkpoints/

# Documentation build
docs/_build/
docs/.doctrees/

# Distribution / packaging
build/
dist/
*.egg-info/
*.egg
wheels/

# Test coverage
.coverage
htmlcov/
.pytest_cache/

# Environment variables
.env
.env.local

# Temporary files
*.tmp
*.temp
*.bak
*.orig

# Allow tracking policy and checkpoint folders for all experiments (override the global '*.pt' and 'runs/' ignore)
!runs/
!runs/**/policy/
!runs/**/policy/**
!runs/**/ckpt/
!runs/**/ckpt/**

# Keep memory checkpoint files ignored (very large)
runs/**/ckpt/**/memory.pt
runs/**/ckpt/memory.pt

# Re-ignore TensorBoard event files and event folders under runs/ (prevent accidental commits)
# `events.out.tfevents.*` is already ignored above, but because `!runs/` un-ignored the runs/ tree,
# explicitly ignore `events` directories under `runs/` to ensure they stay untracked.
runs/**/events/
runs/**/events/**
